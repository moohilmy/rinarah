<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>New Order Notification</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f9f9f9;
        padding: 20px;
        color: #333;
      }
      .container {
        background: #ffffff;
        border-radius: 8px;
        padding: 20px;
        max-width: 600px;
        margin: auto;
        box-shadow: 0 2px 8px rgba(0,0,0,0.05);
      }
      h2 {
        color: #2c3e50;
      }
      p {
        margin: 5px 0;
      }
      .section {
        margin-bottom: 20px;
        border-top: 1px solid #ddd;
        padding-top: 15px;
      }
      ul {
        padding-left: 20px;
      }
      a.button {
        display: inline-block;
        margin-top: 10px;
        padding: 10px 15px;
        background-color: #3498db;
        color: white;
        text-decoration: none;
        border-radius: 4px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2>New Order Received</h2>

      <div class="section">
        <p><strong>Payment ID:</strong> {{stripePaymentIntentId}}</p>
        <p><strong>Status:</strong> {{status}}</p>
        <p><strong>Order Date:</strong> {{createdAt}}</p>
      </div>

      <div class="section">
        <h3>👤 Customer Information</h3>
        <p><strong>Name:</strong> {{shipping.name}}</p>
        <p><strong>Phone:</strong> {{shipping.phone}}</p>
        <p><strong>Email:</strong> {{customerEmail}}</p>
      </div>

      <div class="section">
        <h3>Shipping Address</h3>
        <p>
          {{shipping.address.line1}}<br />
          {{shipping.address.city}}, {{shipping.address.state}}<br />
          {{shipping.address.zip}}, {{shipping.address.country}}
        </p>
      </div>

      <div class="section">
        <h3>Order Items</h3>
        <ul>
          {{#each items}}
            <li>{{this.name}} - Quantity: {{this.quantity}}</li>
          {{/each}}
        </ul>
      </div>

      <div class="section">
        <h3>Payment Details</h3>
        <p><strong>Method:</strong> {{paymentMethod}}</p>
        <p><strong>Total:</strong> ${{amountTotal}}</p>
        <p><strong>Subtotal:</strong> ${{subtotal}}</p>
        <p><strong>Tax:</strong> ${{tax}}</p>
        <p><strong>Shipping Fee:</strong> ${{shipping.shippingPrice}}</p>
      </div>

      <div class="section">
        <h3> Shipping Label</h3>
        <p>
          <a href="{{shippo.labelUrl}}" target="_blank" class="button">
            Download Shipping Label
          </a>
        </p>
      </div>
    </div>
  </body>
</html>
